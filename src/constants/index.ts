export const DIAL_SIZE = 280;
export const DIAL_CENTER = DIAL_SIZE / 2;
export const TICK_RADIUS = 120;
export const TOTAL_TICKS = 60;

export const ITEM_HEIGHT = 52;
export const HOURS_DATA = Array.from({ length: 24 }, (_, i) => i);
export const MINUTES_DATA = Array.from({ length: 60 }, (_, i) => i);
export const SECONDS_DATA = Array.from({ length: 60 }, (_, i) => i);
